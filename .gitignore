# Environment
.env
.env.*
!.env.example
!.env.prod.example

# Telethon sessions (contain Telegram auth — never commit)
*.session
*.session-journal

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
.venv-*/
venv/
venv-*/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
build/
dist/

# Node / frontend
node_modules/
.pnp
.pnp.js
.yarn/
dist/
.vite/
.cache/
coverage/
*.local

# Docker / infra runtime data
data/
volumes/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code local settings
.claude/settings.local.json

# Internal docs (local-only, not for the repo)
docs/
бизнес-требования.md

# AI assistant configs (local-only)
CLAUDE.md
AGENTS.md
.claude/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Test artifacts
playwright-report/
test-results/
.last-run.json

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Xray sidecar — VLESS uuid/host are secrets, never commit the live config
infra/xray/config.json

# Worktrees fallback location (used when EnterWorktree is unavailable in the session)
.worktrees/

# Superpowers brainstorming session artifacts (visual companion mockups)
.superpowers/

# SSH access for ad-hoc prod debugging (keys, host data, captured logs)
.deploy/
